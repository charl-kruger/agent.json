{
  "name": "agentpop",
  "version": "1.0.0",
  "private": true,
  "description": "DNS for AI messages. Give your website an inbox that any agent can reach.",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter agent-inbox dev",
    "dev:docs": "pnpm --filter docs dev",
    "deploy": "pnpm --filter agent-inbox deploy",
    "deploy:docs": "pnpm --filter docs deploy"
  }
}
